{
  "name": "agentapi-proxy-native-dashboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "macOS menu-bar dashboard for the agentapi-proxy native External Session Manager.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "check": "tsc --noEmit",
    "sidecar:build": "bun scripts/build-sidecar.ts",
    "sidecar:build:macos-arm64": "bun scripts/build-sidecar.ts aarch64-apple-darwin",
    "sidecar:build:macos-x64": "bun scripts/build-sidecar.ts x86_64-apple-darwin",
    "version:set": "bun scripts/set-version.ts",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.1.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
