version: '3.8'

services:
  agentapi-proxy:
    build: .
    ports:
      - "8889:8080"
    volumes:
      - agentapi-proxy-home:/home/agentapi
    environment:
      - HOME=/home/agentapi
    working_dir: /home/agentapi
    restart: unless-stopped

volumes:
  agentapi-proxy-home:
