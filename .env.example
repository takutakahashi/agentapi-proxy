# using PAT
#export GITHUB_TOKEN=xxxxxxxxxxxx

# or GitHub App
#export GITHUB_APP_PEM_PATH=/path/to/github_app.pem
#export GITHUB_APP_ID=123

# recommended using --dangerously-skip-permissions, and you can add any other args
export CLAUDE_ARGS="--dangerously-skip-permissions"

# Enable clean architecture controllers (default: false)
# Set to 'true' to use new controller implementations from internal/interfaces/controllers
# This provides better separation of concerns and testability
#export AGENTAPI_USE_CLEAN_CONTROLLERS=true
